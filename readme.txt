Adding router:
Installed react-router-dom
Linked both the main page and search page to each other
Added Routes element to App component.
Created new components for MainPage and Search, each having its Route in App component.

Showing fetched data:
Added API function in App to fetch all books
Sent the books as prop to the MainPage component
Created new components for each bookshelf
Each Bookshelf component gets its corresponding books list via filtering the books prop